More fixes:
SAMPLE PACKS!!!

sample recording from mic;
grid launching;
    compass setup | visualization;

better sync***
    para descobrir o divisor, basta dividir os beats por 2 até que seja <= 2!!!!!
    daí dividir duração do clip pelo divisor ate que seja menor que 8   00ms (75bpm +-);

samplebank:
    > metatag filtering;
        > oneshot | loop | slice;
        > mood tags;
    > estimated bpm;
    > preview listen;

sequence box:
    > div que rola pra baixo da track;
    > empurra tudo pra baixo;
    > sequence bank (mais simples que samplebank);
    > random sequences;

-----------------------------------------------------
right hand menu::
    delete track | all (from DB, too);
    playing groups table::
        stop group + stop all;
    parameter locks(per track + slice index) table::
        pitch;
        reverse;
        duration;
        autorepeat;
        FX;
    sample bank - how to search samples on the web?
recording box::
    timeline;
    render to wav;
slice offset: handlers between slices
Midi Support;
save/load json file | link share;


webpack dev server;                     OK
start/end point;                        OK
groups;                                 OK
keyboard support;                       OK
Reverse: invert start / end points;     OK
BPM;                                    OK
Master Vol.;                            OK
indexedDB: auto restore session;        OK FOR CLIPS
